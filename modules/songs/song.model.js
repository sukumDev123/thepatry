export class SongBandModel {
  constructor(name_band, detail_band, price_band, img_src, path_img) {
    this.name_band = name_band
    this.detail_band = detail_band
    this.price_band = price_band
    this.img_src = img_src
    this.path_img = path_img
  }
}
